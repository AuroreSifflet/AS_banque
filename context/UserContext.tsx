/* import React, { createContext } from "react";
import dataCard from "../js/data.json"; */
// que'est ce qu'on veut que notre contexte porte comme informations
/* export default createContext({
    userSelected: ,
    updateUser: (name) => {}
}); */

/* export const UserProvider = ({ children }) => {
    const [userSelected, setNewuserSelected] = React.useState(dataCard[0].user);
    const result = dataCard.filter(item => item.user === userSelected);


    const [theme, setTheme] = React.useState('light')
    const toggleTheme = () => {
        setTheme(theme === 'light' ? 'dark' : 'light')
    }
 
    return (
        <UserContext.Provider value={{ userSelected, toggleTheme }}>
            {children}
        </UserContext.Provider>
    )
} */